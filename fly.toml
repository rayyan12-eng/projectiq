app = "projectiq-risk"
primary_region = "iad"

[build]

[http_service]
  internal_port = 8002
  force_https = true
  auto_stop_machines = true
  auto_start_machines = true
  min_machines_running = 0

[[vm]]
  size = "shared-cpu-1x"
  memory = "512mb"

[checks]
  [checks.health]
    type = "http"
    port = 8002
    path = "/health"
    interval = "30s"
    timeout = "5s"
